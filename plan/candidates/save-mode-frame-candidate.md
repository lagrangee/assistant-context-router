# Save Mode Frame Candidate

## Purpose
This document describes the runtime save frame and points to the canonical plugin-owned prompt file.

Canonical runtime file:
- `implementation/adapters/openclaw/plugin/prompts/save-mode-frame.md`

This candidate note exists so the save frame can still be reviewed in the documentation layer without mixing review prose into the runtime prompt itself.

## Rules
- The runtime should load the plugin-owned prompt file, not this document.
- This candidate doc may discuss intent, constraints, and future edits.
- Human-readable explanation should never be mixed into the runtime prompt file.
