# Copy to .env for local development. Never commit real values.

# Feishu Base token used by the work-surface adapter.
FEISHU_BASE_TOKEN=

# Optional explicit config hosts. If omitted, the OpenClaw plugin discovers
# config under <dataDir>/assistant-context-router/.
ACR_FEISHU_CONFIG_PATH=
ACR_RUNTIME_BINDINGS_PATH=
