# STATUS

## TL;DR（一句话）
Step 1 与 Step 1.5 已完成并通过真实使用验证；Step 2 当前最小闭环已推进到 `Cut 8E`。Feishu 侧除了 work-surface / governance / catalog / business notification 之外，`Tasks / Bugs` 的 policy schema、writeback first slice、semantic execution bridge、以及 main-session boundary auto-capture 都已经接进 runtime；当前主线已把 `/project` 收口成唯一公开命令入口，并把 `--surface-sync / --catalog-sync / --governance / --notifications` 固化成 project-scoped operating surface。

## 当前阶段（你现在在哪）
- Step 1：Baseline done
- Step 1.5：Baseline done，已完成一轮真实工作流验证
- Step 2：最小闭环的 Cut 1–7 已 implemented + auto-validated，其中 Gate 5 已 human-validated；Cut 8A/8B/8C/8D/8E 当前已分别完成 work-surface projection、`Projects` catalog sync、`Business Notification` 的 Feishu IM delivery、`Tasks / Bugs` 的 policy schema + writeback first slice、以及 semantic execution bridge + main-session boundary auto-capture
- 当前重点：`review_resolution / complete -> Task/Bug row` 的真实闭环已经打通；completion-boundary WeChat DM 也已 live validate 通过；当前应回到更高层的 workflow/product 主线推进，而不是继续卡在 enum/schema 或通知投递问题上
- 当前新增收口：live Base workflow 当前已扩到 `6` 条，除了 `Tasks/Bugs Reviewing -> Done/Fixed/Todo` 的 `review_resolution` 自动回流之外，也已新增 `Tasks Todo -> Doing => dispatch` 与 `Bugs Todo -> Fixing => dispatch` 两条 execution-start workflow；当前 `Task: Todo -> Doing => dispatch` 已 live validate 到 `EXECUTE / in_progress`，Bug path 也已验证 agent-output boundary auto-capture 与 completion-boundary WeChat DM
- 当前新增收口：runtime observer 已收回到最终有效的最小路径：直接使用 Human 本机已 auth 的 `~/.lark-cli/config.json`，并且在显式传入 env 时不再把整份 `process.env` merge 回 child-process。当前真实 `Task: Todo -> Doing => dispatch` 已再次 live validate 通过，card 能自动补齐 `current_step = EXECUTE` 与 `step_result = in_progress`
- 当前最新 Cut 8F 收口：已用 ACR 自己的一条真实 Task 验证 self-hosted semantic execution quality。第一条 live task `<self-host-task-record-overflow>` 已证明 Feishu dispatch / card `EXECUTE in_progress` / main-session semantic request 能触发，但因 main-session prompt context overflow 未到 complete boundary；这不是 auth / enum / notification 旧分支。已落地的通用修复包括：harness context assembly 不再 inline 完整 playbook，`before_prompt_build` 对 pending semantic execution 使用 compact context，只保留 `execution_context / adapter_facts_json / selected_playbooks / boundary schema / row_anchors`，并增加 targeted search 与 repo/package layout guidance。第二条 live retry `<self-host-task-record-complete>` 已完成 Feishu card -> ACR dispatch -> main session semantic execution -> agent-output `[ACR_AUTOMATION] complete` -> card writeback，live row 读回为 `Reviewing / REVIEW_WAIT / need_review`，`执行摘要` 已包含 summary + evidence。当前 local verification：在正确 package host `implementation/adapters/openclaw/plugin` 下运行 `npm test`，`237/237` passed。
- 当前新增收口：`8D` 已证明 work-surface / acceptance 回流闭环成立，但这仍然是 `work surface closure`，不是完整 `semantic execution closure`。`8E` 已从 bridge skeleton 推进到 OpenClaw main-session mediated executor first slice，并且 live [router.yaml](<repo-root>/router.yaml:1) 已切到 `service_binding.runtime_kind=feishu_task_bug_semantic`、`target_ref=agent:main:main`；`dispatch` 会读取真实 card execution context、投递 trusted system event、在目标 main session 写入 `pending_semantic_execution`，并在 runtime 支持时只发起非阻塞 heartbeat request，不等待 main-session heartbeat/model 跑完。main-session agent 后续在 assistant output 中产出 strict `[ACR_AUTOMATION]` boundary block 后，`llm_output` 会在 pending execution 存在时自动捕获并复用现有 route / service / writeback / signal promotion 链；这一步不再需要 project owner 手工复制 `complete`。当前还新增了 complete-boundary fail-closed guard：`complete` 必须带 concrete `summary`，prompt 不再给可直接复制的 complete block，而是给带 placeholder 的 schema，并要求 `evidence`。Bug complete boundary regression 也已补齐：pending `bug_record_id` 会合并回 boundary envelope，`fix_result` 会保留到 service/writeback 链，且 completion-boundary DM 不创建 governance truth。当前 completion-boundary DM 已完成 live validation：`ACR完成提醒 = 完成边界提醒` 可生成 business notification 并送达 WeChat DM；WeChat direct delivery failure 会 fallback 到 OpenClaw main-session delivery，并保留 `/project --notifications` audit。下一步应继续用 ACR 自身跑真实任务，观察 semantic execution context 与 work-surface 操作范式，而不是继续停在通知出口排障。
- 当前最新采纳的产品级 truth：应先按 [feishu-work-surface-operating-model.md](<repo-root>/plan/active/feishu-work-surface-operating-model.md:1) 收紧 work surface 范式；`Todo` 默认是 backlog，不是 dispatch signal，后续 workflow 应逐步从“目标状态触发”收紧为“状态迁移触发”
- 当前补充收口：`start_mode / ACR启动方式` 的最小 contract 已新增，4 条 `review_resolution` workflow 也都已收紧为“修改前必须是 Reviewing 才触发”；`task_bug_policy.defaults.start_mode` 现已进入 runtime config parsing 与 writeback policy resolution
- 新增 live finding：`invalid_protocol_json` 的主 root cause 已确认是 Feishu workflow ingress 到 OpenClaw 时，正文并不总是纯文本，当前 live failure 的真实形状是 rich post wrapper：`body` 带 `[message_id] + sender 前缀`，后面再嵌 `{\"title\":null,\"elements\":[...text...]}`；当前修复已落在 `protocols/ingress-text.ts` 的 transport-specific extractor boundary，由 `before_dispatch.resolveMessageText(...)` 调用 extractor 先解 envelope/rich post，再进 structured parser。parser 现在只保留对少量宿主噪音（`BOM / zero-width / NBSP / CRLF`）的最小归一化，已移除“前置非 JSON 碎片也尝试恢复”的宽松逻辑。同时 workflow sender 若使用 `多维表格助手 / bot` 而群仅响应 Human，则不会进入 ACR，当前短期应采用能稳定工作的 sender 配置，长期应引入显式可信 automation sender allow policy
- 当前新的收口点：
  - 当前 repo 已新增 project-owned [router.yaml](<repo-root>/router.yaml:1)
  - 当前 repo 已新增 validation fixture [validation/service-results.json](<repo-root>/validation/service-results.json:1)
  - 当前 self-hosted live anchor 也已补齐：
    - `Projects` row: `<projects-row-record-id>`
    - validation `Task` row: `<validation-task-record-id>`
  - 第二轮真实 `automation-ingress` structured message 当前已 live-validated：
    - 群里收到：`Accepted dispatch for proj-assistant-context-router`
    - validation `Task` row `<validation-task-record-id>` 已进入：`Doing / EXECUTE / in_progress`
  - `review_request` 当前也已 live-validated：
    - `agent-coordination` 已作为当前默认 `agent_coordination` binding 承接 review 过程
    - `WeChat DM` 已收到 `proj-assistant-context-router` 的 governance escalation
  - 当前已知收口：
    - 全局旧 `dispatch` 规则已由 Human 手工删除
    - ACR parser 已增强，允许 `[ACR_AUTOMATION]` 前存在非开头宿主前缀
    - 当前 `dispatch` first slice 已不再需要额外的 ingress bridge 才能工作
  - 当前新采纳的架构边界：
    - `automation-ingress / agent-coordination` 只是当前默认 binding
    - 长期稳定的是 `automation_ingress / agent_coordination / governance_escalation` 三个逻辑 surface
    - future 可替换为 Discord / 其他 transport，而不改 ACR core contract
    - 当前进一步收口为：
      - `automation_ingress` 按 multi-source external surface 建模
      - `agent_coordination` 先按单一主工作面建模
  - 当前新增的 use case 收口：
    - Human 在 `Tasks / Bugs` card 上对 `Reviewing -> Done/Todo` 的手动决策
    - 应触发 `review_resolution ingress`
    - 作为外部 structured decision input 回流 `automation_ingress`
    - 不应误进入 `agent_coordination`
  - 当前实现新进展：
    - `review_resolution` 已进入 self-host harness
    - writeback 已支持：
      - `accepted -> Done / Fixed`
      - `rejected -> Todo`
    - `complete` 也已进入 self-host harness
      - `manual_acceptance -> Reviewing / REVIEW_WAIT / need_review`
      - `agent_can_finalize -> Done / Fixed / COMPLETE / accepted`
    - 当前 live Base 也已新增并启用 `Tasks` 的两条最小 workflow：
      - `<task-review-accepted-workflow-id>`
        - `Reviewing -> Done => review_resolution / accepted`
      - `<task-review-rejected-workflow-id>`
        - `Reviewing -> Todo => review_resolution / rejected`
    - 对 `Done / Fixed` 的 Human 先手编辑不再被 terminal noop 吞掉
  - 当前下一步：
    - 基于已打通的 `review_resolution / complete -> Task/Bug row` 闭环，继续推进真实 semantic execution 主线
    - 使用 ACR 自身作为第一用户继续跑真实任务，优先观察 context assembly、work-surface 操作与 boundary 收口质量
    - 当前 enum/schema 与 completion-boundary notification blocker 已解除，后续类似问题由 writeback preflight guard 与 delivery outbox audit 提前暴露
- 当前已新增一条 Feishu-related 运行决策：全局 default `governance target` 先定为 `WeChat DM`，但它必须走显式 config binding，不得 hardcode 在 runtime / adapter 代码里
- 当前已新增第一版 Feishu config host runtime path：`work-surface base binding` 已接入默认 runtime path，`governance delivery binding` 也已进入 escalation outbox；同时 runtime bindings 也已收口到默认 `<plugin dataDir>/assistant-context-router/runtime-bindings.yaml` 宿主

## 下一步（从这里继续推进主线）
- 当前已完成的 Cut 6 / Cut 7 收口包括：
  - `minimal-visibility-evidence` contract 已 formalize
  - `ServiceResult` 已支持单个 `artifact_ref`
  - `project session lane` / `business notification` / `main-session escalation` / execution-facing prompt hint 都会保留 `artifact_ref`
  - `/project --lane` 现在会显示最小 evidence 提示
  - `work-surface-projection` contract 已 formalize
  - 高信号 signal 会同步更新 per-project latest work-surface snapshot
  - snapshot 已携带 `headline / summary / run_id / queue_ref / optional artifact_ref`
- 当前已确认的 Feishu 侧起点：
  - 通过 FEISHU_BASE_TOKEN 或 config host 绑定既有 Base：`private config host`
  - Human review 通过后，已真实创建：
    - `Dict Definition.Work Surface状态`
    - `Work Surface Snapshots`
  - live table 当前实际关键字段名为：
    - `Project ID`
    - `所属项目`
    - `状态`
    - `标题`
    - `摘要`
    - `更新时间`
  - manual sync / dry-run 已在真实 Base 上通过一次验证：
    - `proj-bitable-pm-system`
    - 可生成包含 relation / datetime / artifact fields 的 upsert plan
  - main-session plugin 已新增：
    - 首选入口：`/project [<project_id>] --surface-sync [--apply]`
    - 默认 `dry_run`
    - `--apply` 才真正写 Feishu
    - 未传 `project_id` 时使用当前 `/project` binding
    - `/project` 现已成为唯一公开命令入口：
      - `/project --all [query]`
      - `/project [<project_id>] --lane`
      - `/project [<project_id>] --catalog-sync [--apply]`
      - `/project --save`
      - `/project [<project_id>] --surface-sync [--apply]`
      - `/project --help`
    - default runner 当前会先解析统一的 `work-surface binding`
      - 支持 env + optional local config host
      - 若未显式指定，则默认尝试发现 `<plugin dataDir>/assistant-context-router/feishu-adapter.yaml`
      - 若没有显式 binding，现阶段仍保守回退到已确认的 Base token
    - `FEISHU_BASE_TOKEN` 仍可作为最直接的 env override
  - 当前机器的默认 runtime host 已创建：
    - `<openclaw-acr-data-dir>/feishu-adapter.yaml`
    - 已显式包含 `work_surface` 与 `governance.default_target`
  - 当前机器的默认 runtime bindings host 也已创建：
    - `<openclaw-acr-data-dir>/runtime-bindings.yaml`
    - OpenClaw plugin config 已去掉旧的 demo `runtimeBindingsPath`
  - `governance delivery binding` 已接入默认 escalation runtime path
    - 当前先进入幂等 `governance delivery outbox`
    - 再通过 OpenClaw runtime sender 投递到解析后的 canonical session
    - 依赖 `runtimeBindings.main_sessions` alias 解析，例如 `local:human_dm -> wechat:dm:human -> agent:main:main`
    - 当前仍未直接接 WeChat / Feishu 外部 API
  - 当前已新增 project-scoped governance inspect 入口：
    - `/project [<project_id>] --governance`
    - 默认按当前 project binding 查看
    - 展示的是 governance outbox mirror，而不是 governance truth
  - live governance delivery 已真实验证通过：
    - `demo-acr` 的 `blocked` 事项已成功送达 `WeChat DM`
    - `/project --governance` 与 live delivery 状态已对齐
  - 第一次真实 live upsert 也已通过：
    - `create` / `update` 两条路径均已验证
    - 当前验证 record id：`<work-surface-snapshot-record-id>`
  - plugin 当前已具备 optional `workSurfaceProjectionObserver`
    - observer 直接消费真实 snapshot 对象
    - failure safe-fail，不反向影响 ACR 主链
    - 当前不默认自动 apply 到 Feishu
  - `Tasks / Bugs` 的 meta 设计、字段定义、状态机约束是继承参考
  - 第一刀不直接把 project-level snapshot 写进 `Tasks / Bugs`
  - `Service Runs Monitor` 不是当前 projection 的默认目标
  - 后续新增或调整表结构时，仍必须先与 Human 讨论确认
  - 当前已采纳的 Feishu 主线实现顺序：
    - `Projects` catalog sync first slice（已 implemented + auto-validated + live-validated）
    - `Business Notification` 的 Feishu IM delivery（next）
    - `Tasks / Bugs` ownership + acceptance policy contract（next）
    - `proj-assistant-context-router` self-hosted real usage
    - 第一种 `Feishu Action Ingress`
    - `card action / reconcile / backlog projection` 后置
  - 当前 `Tasks / Bugs` 的新判断：
    - `状态` 是 policy-gated shared field，不是纯 Human-owned 或纯 ACR-owned
    - `acceptance_mode` 与 `completion_notify_mode` 应属于 card policy surface
    - 它们应支持 `project default + row-level override`
    - project default 当前建议落在 project-owned `router.yaml`
    - 当前 live Base 已真实创建 row-level 字段：
      - `ACR验收模式`
      - `ACR完成提醒`
    - 当前也已同批补：
      - `ACR开始执行时间`
      - 类型：datetime
      - 语义：首次进入 `Doing / Fixing` 时写入
    - 当前推荐的系统安全默认值：
      - `acceptance_mode = manual_acceptance`
      - `completion_notify_mode = no_dm_on_completion_boundary`
    - 当前 `Task/Bug writeback first slice` 也已 implemented + auto-validated：
      - hook 点位于真实 `service path`
      - 只接受显式 row anchor
      - 当前已真实写入：
        - `current_step / step_result / next_action / last_event_at`
        - `ACR开始执行时间`
        - `执行摘要`（Task）
        - `修复方式`（Bug）
        - `修复结果`（Bug complete 且显式 `fix_result`）
        - `状态` 的非终态推进
      - 当前 guardrails：
        - terminal row 默认 noop
        - 避免 `Reviewing -> Doing/Fixing` 回退
        - 不自动写 `实际完成时间`
        - 不在缺少 explicit `fix_result` 时猜测 `修复结果`
- 保持 Step 2 在 bounded skeleton 范围内，不提前滑入 memory、autonomy、第二 work surface、完整 notification platform 或主动打断式 escalation delivery
